# ShopSphere API Server

Express.js + TypeScript + MongoDB Mongoose backend service for ShopSphere.

## Commands

- `npm run dev` — Start development server with TSX hot reloader
- `npm run build` — Compile TypeScript to dist/
- `npm run start` — Run production server
- `npm run type-check` — Verify strict TypeScript compilation
