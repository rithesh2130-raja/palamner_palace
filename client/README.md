# ShopSphere Client Frontend

React 18 + TypeScript + Vite + Tailwind CSS frontend application for ShopSphere.

## Commands

- `npm run dev` — Start Vite development server on `http://localhost:5173`
- `npm run build` — Build production bundle to `dist/`
- `npm run type-check` — Verify strict TypeScript compilation
